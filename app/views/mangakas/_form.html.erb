<%= form_with(model: mangaka) do |form| %>
  <% if mangaka.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mangaka.errors.count, "error") %> prohibited this mangaka from being saved:</h2>

      <ul>
        <% mangaka.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
