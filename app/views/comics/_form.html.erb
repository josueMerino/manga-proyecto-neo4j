<%= form_with(model: comic) do |form| %>
  <% if comic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comic.errors.count, "error") %> prohibited this comic from being saved:</h2>

      <ul>
        <% comic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :sinopsis %>
    <%= form.text_field :sinopsis %>
  </div>

  <div class="field">
    <%= form.label :generos %>
    <%= form.text_field :generos %>
  </div>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.number_field :is_active %>
  </div>

  <div class="field">
    <%= form.label :publish_date %>
    <%= form.date_select :publish_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
